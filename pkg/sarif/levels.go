// Package sarif levels has an enum of the possible sarif finding levels
package sarif

const (
	// LevelError is Sarif finding level "error"
	LevelError = "error"
	// LevelWarning is Sarif finding level "warning"
	LevelWarning = "warning"
	// LevelNote is Sarif finding level "note"
	LevelNote = "note"
	// LevelNone is Sarif finding level without a level
	LevelNone = "none"
)
