# DO NOT EDIT. Code generated by:
# github.com/ocurity/dracon/bin/kustomize-component-generator.

apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - task.yaml
patches:
  # Add the Task to the Tekton Pipeline.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        workspaces:
          - name: source-code-ws
        tasks:
          - name: producer-typescript-eslint
            taskRef:
              name: producer-typescript-eslint
            workspaces:
              - name: source-code-ws
                workspace: source-code-ws
            params:
              - name: producer-typescript-eslint-config-js
                value: $(params.producer-typescript-eslint-config-js)
        params:
          - name: producer-typescript-eslint-config-js
            type: string
            default: ""
    target:
      kind: Pipeline
  # Add anchors to Task.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Task
      metadata:
        name: producer-typescript-eslint
        labels:
          v1.dracon.ocurity.com/component: producer
      spec:
        params:
          - name: anchors
            type: array
            description: A list of tasks that this task depends on using their anchors.
            default: []
        results:
          - name: anchor
            description: An anchor to allow other tasks to depend on this task.
        steps:
          - name: anchor
            image: docker.io/busybox:1.35.0
            script: echo "$(context.task.name)" > "$(results.anchor.path)"
    target:
      kind: Task
      name: producer-typescript-eslint
  # If we have a `source` task in the pipeline (added by a `source` component),
  # depend on the completion of that source by referencing its anchor.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        tasks:
          - name: producer-typescript-eslint
            params:
              - name: anchors
                value:
                  - $(tasks.source.results.anchor)
    target:
      kind: Pipeline
      annotationSelector: v1.dracon.ocurity.com/has-source=true
  # If we have a producer-aggregator task in the pipeline (added by the
  # producer-aggregator component), make it depend on the completion of this
  # producer.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        tasks:
          - name: producer-aggregator
            params:
              - name: anchors
                value:
                  - $(tasks.producer-typescript-eslint.results.anchor)
    target:
      kind: Pipeline
      annotationSelector: v1.dracon.ocurity.com/has-producer-aggregator=true
  # Add scan information to Task.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Task
      metadata:
        name: producer-typescript-eslint
        labels:
          v1.dracon.ocurity.com/component: producer
      spec:
        params:
          - name: dracon_scan_id
            type: string
          - name: dracon_scan_start_time
            type: string
        steps:
          - name: run-eslint
            image: '{{ default "ghcr.io/ocurity/dracon" .Values.container_registry }}/components/producers/typescript-eslint/eslint-wrapper/eslint:{{ default "latest" .Values.dracon_os_component_version }}'
            env:
              - name: DRACON_SCAN_TIME
                value: $(params.dracon_scan_start_time)
              - name: DRACON_SCAN_ID
                value: $(params.dracon_scan_id)
          - name: produce-issues
            image: '{{ default "ghcr.io/ocurity/dracon" .Values.container_registry }}/components/producers/typescript-eslint:{{ default "latest" .Values.dracon_os_component_version }}'
            env:
              - name: DRACON_SCAN_TIME
                value: $(params.dracon_scan_start_time)
              - name: DRACON_SCAN_ID
                value: $(params.dracon_scan_id)
    target:
      kind: Task
      name: producer-typescript-eslint
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        tasks:
          - name: producer-typescript-eslint
            params:
              - name: dracon_scan_id
                value: $(tasks.base.results.dracon-scan-id)
              - name: dracon_scan_start_time
                value: $(tasks.base.results.dracon-scan-start-time)
    target:
      kind: Pipeline
