# DO NOT EDIT. Code generated by:
# github.com/ocurity/dracon//build/tools/kustomize-component-generator.

apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - task.yaml
patches:
  # Add the Task to the Tekton Pipeline.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        workspaces:
          - name: source-code-ws
        tasks:
          - name: consumer-dependency-track
            taskRef:
              name: consumer-dependency-track
            workspaces:
              - name: source-code-ws
                workspace: source-code-ws
            params:
              - name: consumer-dependency-track-api-url
                value: $(params.consumer-dependency-track-api-url)
              - name: consumer-dependency-track-project-name
                value: $(params.consumer-dependency-track-project-name)
              - name: consumer-dependency-track-project-version
                value: $(params.consumer-dependency-track-project-version)
              - name: consumer-dependency-track-token
                value: $(params.consumer-dependency-track-token)
              - name: consumer-dependency-track-project-uuid
                value: $(params.consumer-dependency-track-project-uuid)
        params:
          - name: consumer-dependency-track-api-url
            type: string
          - name: consumer-dependency-track-project-name
            type: string
          - name: consumer-dependency-track-project-version
            type: string
          - name: consumer-dependency-track-token
            type: string
          - name: consumer-dependency-track-project-uuid
            type: string
    target:
      kind: Pipeline
  # Add anchors to Task.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Task
      metadata:
        name: consumer-dependency-track
        labels:
          v1.dracon.ocurity.com/component: consumer
      spec:
        params:
          - name: anchors
            type: array
            description: A list of tasks that this task depends on using their anchors.
            default: []
        results:
          - name: anchor
            description: An anchor to allow other tasks to depend on this task.
        steps:
          - name: anchor
            image: docker.io/busybox:1.35.0
            script: echo "$(context.task.name)" > "$(results.anchor.path)"
    target:
      kind: Task
      name: consumer-dependency-track
  # If we have an enricher-aggregator task in the pipeline (added by the
  # enricher-aggregator component), make the consumer depend on the completion of
  # it.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        tasks:
          - name: consumer-dependency-track
            params:
              - name: anchors
                value:
                  - $(tasks.enricher-aggregator.results.anchor)
    target:
      kind: Pipeline
      annotationSelector: v1.dracon.ocurity.com/has-enricher-aggregator=true
