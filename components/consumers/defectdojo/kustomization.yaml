# DO NOT EDIT. Code generated by:
# github.com/ocurity/dracon/bin/kustomize-component-generator.

apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - task.yaml
patches:
  # Add the Task to the Tekton Pipeline.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        workspaces:
          - name: source-code-ws
        tasks:
          - name: consumer-defectdojo
            taskRef:
              name: consumer-defectdojo
            workspaces:
              - name: source-code-ws
                workspace: source-code-ws
            params:
              - name: consumer-defectdojo-api-url
                value: $(params.consumer-defectdojo-api-url)
              - name: consumer-defectdojo-user
                value: $(params.consumer-defectdojo-user)
              - name: consumer-defectdojo-token
                value: $(params.consumer-defectdojo-token)
              - name: consumer-defectdojo-product-id
                value: $(params.consumer-defectdojo-product-id)
              - name: consumer-defectdojo-description-template
                value: $(params.consumer-defectdojo-description-template)
        params:
          - name: consumer-defectdojo-api-url
            type: string
          - name: consumer-defectdojo-user
            type: string
          - name: consumer-defectdojo-token
            type: string
          - name: consumer-defectdojo-product-id
            type: string
          - name: consumer-defectdojo-description-template
            type: string
            default: ""
    target:
      kind: Pipeline
  # Add anchors to Task.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Task
      metadata:
        name: consumer-defectdojo
        labels:
          v1.dracon.ocurity.com/component: consumer
      spec:
        params:
          - name: anchors
            type: array
            description: A list of tasks that this task depends on using their anchors.
            default: []
        results:
          - name: anchor
            description: An anchor to allow other tasks to depend on this task.
        steps:
          - name: anchor
            image: docker.io/busybox:1.35.0
            script: echo "$(context.task.name)" > "$(results.anchor.path)"
    target:
      kind: Task
      name: consumer-defectdojo
  # If we have an enricher-aggregator task in the pipeline (added by the
  # enricher-aggregator component), make the consumer depend on the completion of
  # it.
  - patch: |
      apiVersion: tekton.dev/v1beta1
      kind: Pipeline
      metadata:
        name: unused
      spec:
        tasks:
          - name: consumer-defectdojo
            params:
              - name: anchors
                value:
                  - $(tasks.enricher-aggregator.results.anchor)
    target:
      kind: Pipeline
      annotationSelector: v1.dracon.ocurity.com/has-enricher-aggregator=true
